import React from 'react';

// This component creates space at the top of the page to compensate for the fixed navbar
// This prevents content from being hidden behind the navbar
const NavbarSpacer = () => {
  return <div className="h-24"></div>;
};

export default NavbarSpacer;
